# environment.lock.yml — snapshot generated from environment.yml on 2025-10-31
name: onco-sentry
channels: [conda-forge, defaults]
dependencies:
  - python>=3.8
  - pip
  - pip:
      - -r requirements.txt
